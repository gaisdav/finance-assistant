export interface ICalendarDM {
  readonly date: number;
  readonly day: number;
  month: number;
  year: number;

  readonly currentDate: number;
  readonly currentDay: number;
  currentMonth: number;
  currentYear: number;
}
